#!/usr/bin/python3    

name = input('Type your name: ')             # The input() function always returns data as a string.
birth_year = int(input('Type Birth_year: '))  # converting the result into an integer by calling the built-in int() function. 
print('typed name and age are : ', name,birth_year)
